Training reports are generated locally or in the approved private training
environment and committed only after review. Each report must include
per-class `precision`, `recall`, and `map50`, plus disjoint train/test session
lists. No model weights or dataset images belong in this directory.
