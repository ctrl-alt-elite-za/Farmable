# Vision model handoff

Issue #16 may add model metadata and platform-specific model assets here. Do not add private field imagery or model weights in this authentication change. The mobile app exposes `VisionService` as an integration point only.
