<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!--
      `flutter create` declares INTERNET only in the debug and profile
      manifests, so a RELEASE build ships with no network access at all and
      every API call fails silently. Debug builds work, which is what makes it
      easy to miss until a release APK is in someone's hands.

      Almanac is offline-first, not offline-only: it syncs, fetches prices and
      talks to the planner whenever a connection exists.
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      Device capabilities (#4). Declared HERE, in the main manifest, so a
      release build has them — the debug manifest is not merged into release.

      None of these is requested at launch. Each is asked for by the feature
      that needs it, when it needs it, after the app has said why. Android's
      own prompt shows no app text, so the reasons live in
      res/values/strings.xml and the app shows them first:

        CAMERA        "To scan your crops and animals"  (permission_camera_reason)
        RECORD_AUDIO  "To talk to the assistant"         (permission_microphone_reason)
        LOCATION      "To map your farm sections"        (permission_location_reason)

      test/device/permission_copy_test.dart holds these, the strings file,
      Info.plist and lib/domain/device/permission_copy.dart to one wording.
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!--
      All optional, so the app still installs on a phone without one. The
      self-test reports "not supported on this phone" for what is missing.
    -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.ar" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <!-- The camera plugin declares camera.any as required, and the location
         permissions imply android.hardware.location; both are overridden. -->
    <uses-feature android:name="android.hardware.camera.any" android:required="false" tools:replace="android:required" />
    <uses-feature android:name="android.hardware.location" android:required="false" />

    <!--
      Merged in by libraries for things this app does not do, so removed:
      the camera plugin asks for external storage to save videos to the
      gallery (nothing is saved there), and ML Kit's firebase-iid dependency
      asks to receive push messages (there are none).
    -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" tools:node="remove" />
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" tools:node="remove" />

    <application
        android:label="Almanac"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config">
        <!--
          AR is optional: phones without ARCore install and run the app, and
          the self-test reports AR as not supported rather than failing.
        -->
        <meta-data android:name="com.google.ar.core" android:value="optional" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
