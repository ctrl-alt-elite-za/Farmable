# Reviewed forecast results

Place each reviewed normalized snapshot in `<run_id>/forecast.json`, using the
contract in `docs/forecast-outlook.md`. No results are published by default.
Synthetic test fixtures remain outside this directory and are never copied into
the deployment import image. Do not put credentials or raw training data here.
